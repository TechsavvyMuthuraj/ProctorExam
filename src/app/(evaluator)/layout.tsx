
import { EvaluatorLayout } from '@/components/evaluator-layout';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <EvaluatorLayout>{children}</EvaluatorLayout>;
}
